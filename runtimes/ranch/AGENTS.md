# Wrangle Ranch instructions

Maintain [CAPABILITIES.md](CAPABILITIES.md) when intentionally supported Ranch behavior changes. Keep canonical configuration ownership in the Durable Data Server and live connection testing in Ranch.

Run `npm start` and `npm test` from this package. Ranch reaches the working-name `authority-server` package through declared HTTP artifacts and must not publish connection state to Router.
