# Wrangle Gallery instructions

Maintain [CAPABILITIES.md](CAPABILITIES.md) when intentionally supported Gallery behavior changes. Do not infer discovery, request, or authority functionality from identity and health.

Run `npm start` and `npm test` from this package. Declare shared-package dependencies in this package manifest.
