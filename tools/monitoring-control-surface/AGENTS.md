# Monitoring / Control Surface instructions

This package is an exploratory side program, not an Agent Wrangler runtime. Keep monitoring read-only and scenario control explicit about claimed ownership and endpoint knowledge.

Use runtime-specific CLI commands for scenario operations. Do not read runtime-private storage, acquire durable product configuration, or imply that simulated actor claims provide authorization.

Maintain `CAPABILITIES.md` when intentionally supported behavior changes. Keep tests focused on advertised happy flows.

