export const executionNodeHttp = Object.freeze({
  schemaVersion: "temporary-execution-node-http/v1",
  producer: "execution-node",
  paths: Object.freeze({ capabilities: "/capabilities", execute: "/execute" }),
});
