export const runtimeDiagnosticsHttp = Object.freeze({
  schemaVersion: "temporary-runtime-diagnostics-http/v1",
  producer: "each-runtime",
  paths: Object.freeze({ identity: "/identity", health: "/health" }),
});
